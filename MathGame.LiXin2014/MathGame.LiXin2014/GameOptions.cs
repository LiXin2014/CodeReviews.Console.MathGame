namespace MathGame.LiXin2014
{
    // An enum to represent the different options in the game
    public enum GameOptions
    {
        Plus = 1,
        Minus = 2,
        Multiply = 3,
        Divide = 4,
        SetDifficultyLevel = 5,
        ViewDifficultyLevel = 6,
        History = 7,
        Exit = 8,
    }

    // An enum to represent the different operators in the game
    public enum Operators
    {
        Plus = 1,
        Minus = 2,
        Multiply = 3,
        Divide = 4,
    }

    // An enum to represent the difficulty level in the game
    public enum Difficulty
    {
        Easy = 1,
        Medium = 2,
        Hard = 3,
    }
}

    
